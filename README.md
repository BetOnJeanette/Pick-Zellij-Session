# What is this?
This is a small fish script that loads all of the names of existing Zellij sessions into fzf, and if the user picks one, launches into it

# Setup
Move pick_multi_session.fish into .config/fish/functions/

# Usage
Run `pick_multi_session` in fish.
